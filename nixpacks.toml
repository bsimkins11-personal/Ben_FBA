[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[variables]
PYTHONPATH = "."

[start]
cmd = "PYTHONPATH=. uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}"
